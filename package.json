{
  "name": "screensurf",
  "version": "1.0.0",
  "license": "ISC",
  "scripts": {
    "start": "node server.js",
    "dev:server": "nodemon server.js",
    "dev:client": "cd react && npm run dev",
    "build": "cd react && npm install && npm run build",
    "install:all": "npm install && cd react && npm install"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.0",
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
