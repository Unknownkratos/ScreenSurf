# Server Port
PORT=5000

# Production CORS Configuration
# In production, set this to your actual domain(s), comma-separated
# Example: ALLOWED_ORIGINS=https://screensurf.com,https://www.screensurf.com
# If not set in production, defaults to allowing all origins (not recommended)
ALLOWED_ORIGINS=